# 提交前检查

1. 在 WSL 解压工程并进入目录。
2. 执行 `./run_all.sh`。
3. 确认终端中所有测试均为 `PASS`。
4. 确认 `results/all_benchmarks.csv` 和 `results/performance.png` 已生成。
5. 报告可使用 `report/REPORT.md`，并将性能表替换为本机结果。
6. 提交源代码、Makefile、README、测试结果、性能图和实验报告。
