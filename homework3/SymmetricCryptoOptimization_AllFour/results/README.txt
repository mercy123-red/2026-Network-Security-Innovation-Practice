The CSV files and performance.png are generated by:

    make benchmark
    make plot

The bundled results were produced in the validation environment. Re-run
./run_all.sh in the target WSL environment before using numbers in a report.
